{
  "name": "@hashhive/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Shared types and utilities for HashHive",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "lint": "eslint src --ext .ts",
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "test": "echo 'No tests yet' && exit 0"
  },
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
